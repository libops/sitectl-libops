version: 2
before:
  hooks:
    - ./scripts/check-sitectl-core-version.sh v0.38.0
    - go mod tidy
builds:
  - binary: sitectl-libops
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{ .Version }} -X main.commit={{ .Commit }} -X main.date={{ .Date }}
    goos:
      - linux
      - windows
      - darwin

archives:
  - format: tar.gz
    # this name template makes the OS and Arch compatible with the results of uname.
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    # use zip for windows archives
    format_overrides:
      - goos: windows
        format: zip
checksum:
  name_template: "checksums.txt"
snapshot:
  name_template: "{{ incpatch .Version }}-next"
changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^.github"
brews:
  - name: sitectl-libops
    homepage: "https://github.com/libops/sitectl-libops"
    commit_author:
      name: sitectl-dev[bot]
      email: 2408410+sitectl-dev[bot]@users.noreply.github.com
    repository:
      owner: libops
      name: homebrew
    dependencies:
      - name: "libops/homebrew/sitectl"
nfpms:
  - id: sitectl-libops
    package_name: sitectl-libops
    file_name_template: "{{ .ConventionalFileName }}"
    vendor: libops
    homepage: "https://github.com/libops/sitectl-libops"
    description: "A sitectl plugin for LibOps platform operations"
    maintainer: "Joe Corall <joe@libops.io>"
    license: "MIT"
    formats:
      - apk
      - deb
      - rpm
    mtime: "{{ .CommitDate }}"
    builds:
      - sitectl-libops
    dependencies:
      - sitectl
    overrides:
      deb:
        dependencies:
          - sitectl (>= 0.38.0)
      rpm:
        dependencies:
          - sitectl >= 0.38.0
      apk:
        dependencies:
          - sitectl>=0.38.0
